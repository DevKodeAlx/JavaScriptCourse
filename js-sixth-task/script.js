
// Get a reference to the main section.

const mainSection = document.querySelector('.maincontent');

// Create a new text node with the desired text. 

const textNode = document.createTextNode("Ad astra per aspera.");

// Append the text node to the main section. 

mainSection.appendChild(textNode);